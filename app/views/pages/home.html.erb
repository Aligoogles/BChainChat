<% if current_user %>
    <h1><%= current_user.email %> [<%= current_user.role %>]</h1>

    <% if current_user.admin? %>
        Currently there are <%= User.count %> registered users on BChainChat!
        <br />
        <%= link_to 'Review Event Submissions', api_v1_posts_path %>
    <% end %>
    <br />
    <br />
    <%= link_to 'posts', api_v1_posts_path %>
    <br />
    <br />
    <%= link_to "Sign out", destroy_user_session_path,'data-turbo-method': :delete %>
    <br/>

    <% else %>
        <p>You are not logged in </p>
        <%= link_to "Sign up", new_user_registration_path %>
        <br/>
        <%= link_to "Sign in", new_user_session_path %>
<% end %>

<%= @application.as_json %>


